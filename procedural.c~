#include <stdio.h>
#include <stdlib.h>
#include <SDL.h>
#define COL 20
#define LIG 20

int main()
{
  /* initialize SDL */
  SDL_Init(SDL_INIT_VIDEO);

  /* create window */
  screen = SDL_SetVideoMode(COL*32, LIG*32, 0, 0);

  
}
