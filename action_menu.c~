#include "include.h"

perso action_menu(int action, sol tab[COL][LIG], SDL_Surface *screen,perso perso, liste_point *L, int buttx, int butty, int *cond, int zoom)
{
	int  test;
	switch(action)
	{
		case 1:
			if(cond)
				{
					perso = deplacement_personnage(sol,screen,perso,L,buttx,butty,cond,zoom);
				}
			break;			
	}
	return perso;
}
